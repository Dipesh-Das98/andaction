version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Cleaning npm cache"
        - rm -rf node_modules package-lock.json ~/.npm
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths: []
